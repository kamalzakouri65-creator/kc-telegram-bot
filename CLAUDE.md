# ذاكرة المشروع — نظام بوتات KC للتداول

> ملاحظة: هذا الملف يُقرأ تلقائياً في بداية كل محادثة جديدة مع Claude Code، لذا يُستخدم
> كذاكرة دائمة لمتابعة العمل على بوتات التداول (KC Gold V2 و KC PA Bot) حتى لو لم تكن
> أكوادها ضمن هذا المستودع (`kc-telegram-bot`) مباشرة.

## البوتات والملفات

- **V2**: `C:\Users\user\KC_Gold_Bot_V2\kc_gold_v2.py` — Magic: `20260714`
- **PA Bot**: `C:\Users\user\KC_PA_Bot\kc_pa_bot.py` — Magic: `20260725`
- **Config V2**: `config_v2.json`
- **State V2**: `state_v2.json`

## الحساب الحقيقي

- **الوسيط**: XM Global
- **Login**: `302118197`
- **Symbol**: `GOLD#`
- **الرصيد التقريبي**: ~$48.69
- **Telegram Chat ID**: `5984116587`

## الجلسات (بتوقيت UTC)

- طوكيو: 00:00 - 05:00
- لندن: 07:00 - 11:00
- نيويورك: 13:00 - 17:00

## آخر الإصلاحات (2026-07-28)

- **PA Bot SL**: أصبح يُحسب الآن من H1 OB بدلاً من شمعة M5.
- **PA Bot OB**: يجب أن يكون السعر داخل المنطقة (Order Block) فعلياً قبل اعتبارها صالحة.
- **V2 + PA**: جميع الإصلاحات السابقة لا تزال محفوظة وسارية.

## إصلاحات معلّقة (لم تُطبَّق بعد على الأكواد المحلية) — 2026-07-28

### KC_PA_Bot.py
1. **منطق الاتجاه معكوس** (خطر — يفسر صفقات BUY الخاسرة): الشرط الأصلي
   `price >= ema_val - tolerance` → BUY يُفعَّل حتى في الترند الهابط. الإصلاح:
   استخدام `price > ema_val + tolerance` لـ BUY و`price < ema_val - tolerance` لـ SELL،
   مع `direction = None` داخل منطقة التذبذب.
2. **Break Even بمسافة تافهة** (0.1 فقط): يجب ربطها بـ ATR
   (`be_buffer = max(atr_val * be_atr_mult, 0.5)`) بدل رقم ثابت.
3. **Trailing Step ثابت بلا ATR**: `trailing_step_pts * 0.01 = 0.1` ضيق جداً على GOLD.
   الإصلاح: اعتماد `trailing_atr_mult` بدل القيمة الثابتة.
4. **لا فلتر ليوم العطلة**: `in_session()` يتحقق من الساعة فقط، لا يتحقق من
   `weekday() >= 5` (سبت/أحد).
5. **`_daily_pnl` و`_cons_losses` غير محدَّثين فعلياً** (خطر — لا حدود خسارة حقيقية):
   لا توجد دالة تشبه `check_closed_trades()` من V2 لتحديثهما عند إغلاق الصفقات.

### KC_Gold_Bot V2.py
6. **Hard max risk مرتفع جداً**: `balance * 0.15` (15%) — يُفضَّل خفضه إلى ~2%
   خصوصاً مع رصيد صغير (~$48.69).
7. **`bos_confirmed` غامض بسبب أولوية العمليات**: يحتاج أقواس صريحة حول
   `(struct["bos_bull"] or struct["trend"] == "BULLISH")` لكل فرع.
8. **`struct` قد يكون غير معرَّف عند `h1 is None`**: يُستخدم لاحقاً في
   `write_drawings(...)` خارج الشرط `if h1 is not None:` مما قد يسبب استخدام قيمة
   قديمة أو خطأ. الإصلاح: تهيئة `struct = None` قبل الحلقة والتخطي إذا بقي `None`.

**أولوية التطبيق**: #1 و#5 أولاً (مصدر خسارة فعلية بفلوس)، ثم #2/#3/#6، وأخيراً #7/#8.
تُطبَّق هذه الإصلاحات يدوياً على الأكواد المحلية (غير موجودة في هذا المستودع)، ثم تُنقل
إلى قسم "آخر الإصلاحات" أعلاه بعد التأكيد.

## طريقة الاستخدام

في أي محادثة جديدة، يكفي إرسال التقارير أو نتائج التداول مباشرة — يقرأ Claude هذا الملف
تلقائياً ويكون على اطّلاع كامل بالبوتات والحسابات والإعدادات أعلاه دون تكرار الشرح.
